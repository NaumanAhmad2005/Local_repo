to Create a new repo in cmd:

echo "# Local_repo" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin [link of that repo]
git push -u origin main


to push already existing repo from cmd:

git remote add origin [link of that repo]
git branch -M main
git push -u origin main